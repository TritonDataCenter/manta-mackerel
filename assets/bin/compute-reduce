#!/bin/bash
# Copyright (c) 2013, Joyent, Inc. All rights reserved.

set -o pipefail

dir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd $dir/.. && tar -xf node_modules.tar

$dir/../lib/compute-reduce.js \
| $dir/../lib/deliver-usage.js
